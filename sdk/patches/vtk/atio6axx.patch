--- a/Rendering/OpenGL2/CMakeLists.txt
+++ b/Rendering/OpenGL2/CMakeLists.txt
@@ -398,3 +398,11 @@ if (NOT ANDROID AND
     TARGETS vtkProbeOpenGLVersion
     MODULES VTK::RenderingOpenGL2)
 endif ()
+
+# Avoid crash with some versions of atio6axx.dll
+if (WIN32)
+  set_property(SOURCE vtkOpenGLRenderTimer.cxx APPEND
+    PROPERTY
+      COMPILE_DEFINITIONS NO_TIMESTAMP_QUERIES
+  )
+endif()
